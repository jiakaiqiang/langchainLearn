// 旧版入口已迁移到 src/main.ts（按章节运行 demo）
// 运行方式：pnpm dev <demo-name>
// 详见 README.md
console.log("入口已迁移。请使用 pnpm dev <demo-name> 运行对应章节。");
console.log("可用 demo: llm, prompt, structured, chain, tools, agent, memory, rag, streaming, final");
